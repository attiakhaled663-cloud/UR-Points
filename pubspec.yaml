name: ur_points

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.1.0
  cloud_firestore: ^5.0.1
  device_info_plus: ^10.1.0
  webview_flutter: ^4.7.0
  shared_preferences: ^2.2.3
  http: ^1.2.1
  html: ^0.15.4
